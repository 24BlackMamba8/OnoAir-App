export interface Destination {
  name: string;
  imageUrl: string;
  airportName: string;
  airportWebsite: string;
  emailAddress: string;
  destinationCode: string; // Three-letter destination code (uppercase)
}
